{
  "build": {
    "buildCommand": "composer install --optimize-autoloader --no-dev && npm ci && npm run prod"
  },
  "deploy": {
    "startCommand": "php artisan migrate --force && php -S 0.0.0.0:$PORT -t public"
  }
}
